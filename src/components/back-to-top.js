import { Link as HTMLLink } from '@theme-ui/components';

const BackToTop = () => (
  <HTMLLink href="#top" variant="links.navigation">
    &#8682; {'Back to top'}
  </HTMLLink>
);

export default BackToTop;
